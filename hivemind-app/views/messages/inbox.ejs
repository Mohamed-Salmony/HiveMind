<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HiveMind - Messages</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <div class="container header-content">
            <div class="logo">Hive<span>Mind</span></div>
            <nav>
                <ul>
                    <li><a href="/observation/dashboard">Dashboard</a></li>
                    <li><a href="/observation/new">New Observation</a></li>
                    <li><a href="/observation/history">History</a></li>
                    <li><a href="/account">My Account</a></li>
                    <li><a href="/messages">Messages</a></li>
                    <li><a href="/logout">Logout</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="container">
            <div class="card">
                <h1 class="card-title">Messages</h1>
                
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                <% } %>
                
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                    <div>
                        <a href="/messages" class="<%= typeof activeTab === 'undefined' || activeTab === 'inbox' ? 'btn' : 'btn btn-secondary' %>">Inbox</a>
                        <a href="/messages/sent" class="<%= activeTab === 'sent' ? 'btn' : 'btn btn-secondary' %>">Sent</a>
                    </div>
                    <a href="/messages/new" class="btn">New Message</a>
                </div>
                
                <% if (messages.length > 0) { %>
                    <ul class="message-list">
                        <% messages.forEach(message => { %>
                            <li class="message-item <%= !message.read && activeTab !== 'sent' ? 'unread' : '' %>">
                                <div class="message-header">
                                    <% if (activeTab === 'sent') { %>
                                        <span class="message-sender">To: <%= message.recipient.forename %> <%= message.recipient.surname %></span>
                                    <% } else { %>
                                        <span class="message-sender">From: <%= message.sender.forename %> <%= message.sender.surname %></span>
                                    <% } %>
                                    <span class="message-date"><%= new Date(message.timestamp).toLocaleString() %></span>
                                </div>
                                <div class="message-subject">
                                    <a href="/messages/<%= message._id %>"><%= message.subject %></a>
                                </div>
                            </li>
                        <% }); %>
                    </ul>
                <% } else { %>
                    <p>No messages found.</p>
                <% } %>
            </div>
        </div>
    </main>

    <footer>
        <div class="container footer-content">
            <div class="footer-section">
                <h3>HiveMind</h3>
                <p>Enhancing humanity's collective knowledge through universal internet access.</p>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="/observation/dashboard">Dashboard</a></li>
                    <li><a href="/observation/new">New Observation</a></li>
                    <li><a href="/account">My Account</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Contact</h3>
                <p>Email: support@hivemind.org</p>
                <p>Phone: +1 (555) 123-4567</p>
            </div>
        </div>
        <div class="container" style="text-align: center; margin-top: 1rem;">
            <p>&copy; 2025 HiveMind. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
