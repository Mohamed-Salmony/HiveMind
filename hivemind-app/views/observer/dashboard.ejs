<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HiveMind - Observer Dashboard</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <header>
        <div class="container header-content">
            <div class="logo">Hive<span>Mind</span></div>
            <nav>
                <ul>
                    <li><a href="/observation/dashboard">Dashboard</a></li>
                    <li><a href="/observation/new">New Observation</a></li>
                    <li><a href="/observation/history">History</a></li>
                    <li><a href="/account">My Account</a></li>
                    <li><a href="/messages">Messages</a></li>
                    <li><a href="/logout">Logout</a></li>
                </ul>
            </nav>
        </div>
    </header>

    <main>
        <div class="container">
            <div class="card">
                <h1 class="card-title">Observer Dashboard</h1>
                
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                <% } %>
                
                <div class="dashboard-grid">
                    <div class="card stat-card">
                        <h3>Account Balance</h3>
                        <div class="stat-value">$<%= user.accountBalance.toFixed(2) %></div>
                        <div class="stat-label">Current Balance</div>
                    </div>
                    
                    <div class="card stat-card">
                        <h3>Account Status</h3>
                        <div class="stat-value status-<%= user.accountStatus %>"><%= user.accountStatus.charAt(0).toUpperCase() + user.accountStatus.slice(1) %></div>
                        <div class="stat-label">Current Status</div>
                    </div>
                    
                    <div class="card stat-card">
                        <h3>Observations</h3>
                        <div class="stat-value"><%= observations.length %></div>
                        <div class="stat-label">Recent Submissions</div>
                    </div>
                </div>
                
                <div class="card">
                    <h2>Recent Observations</h2>
                    <% if (observations.length > 0) { %>
                        <table>
                            <thead>
                                <tr>
                                    <th>Date</th>
                                    <th>Time</th>
                                    <th>Location (W3W)</th>
                                    <th>Temperature</th>
                                    <th>Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% observations.forEach(observation => { %>
                                    <tr>
                                        <td><%= observation.date %></td>
                                        <td><%= observation.time %></td>
                                        <td><%= observation.coordinates.w3w %></td>
                                        <td><%= observation.temperature %>°C</td>
                                        <td>
                                            <a href="/observation/<%= observation._id %>">View</a>
                                        </td>
                                    </tr>
                                <% }); %>
                            </tbody>
                        </table>
                    <% } else { %>
                        <p>No observations submitted yet. <a href="/observation/new">Submit your first observation</a>.</p>
                    <% } %>
                </div>
                
                <div class="card">
                    <h2>Quick Actions</h2>
                    <div style="display: flex; gap: 1rem; margin-top: 1rem;">
                        <a href="/observation/new" class="btn">Submit New Observation</a>
                        <a href="/messages" class="btn btn-secondary">Check Messages</a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer>
        <div class="container footer-content">
            <div class="footer-section">
                <h3>HiveMind</h3>
                <p>Enhancing humanity's collective knowledge through universal internet access.</p>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="/observation/dashboard">Dashboard</a></li>
                    <li><a href="/observation/new">New Observation</a></li>
                    <li><a href="/account">My Account</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Contact</h3>
                <p>Email: support@hivemind.org</p>
                <p>Phone: +1 (555) 123-4567</p>
            </div>
        </div>
        <div class="container" style="text-align: center; margin-top: 1rem;">
            <p>&copy; 2025 HiveMind. All rights reserved.</p>
        </div>
    </footer>
</body>
</html>
